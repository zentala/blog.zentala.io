type Props = {
  tags: string[],
}

const Tags: React.FC<Props>  = ({ tags }) => {
  // const listTags = tags.map((tag) =>
  //   <li>{tag}</li>
  // );

  // return (
  //   <ul className=''></ul>
  // );
  return (<div/>);
};

export default Tags;
